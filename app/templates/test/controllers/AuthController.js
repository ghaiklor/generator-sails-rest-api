var assert = require('assert'),
    AuthController = require('../../api/controllers/AuthController');

describe("controllers:AuthController", function () {
    it("Should properly sign in", function () {
        assert(false);
    });

    it("Should properly sign up", function () {
        assert(false);
    });

    it("Should properly sign in via facebook", function () {
        assert(false);
    });

    it("Should properly link facebook account", function () {
        assert(false);
    });

    it("Should properly sign in via twitter", function () {
        assert(false);
    });

    it("Should properly link twitter account", function () {
        assert(false);
    });
});
