var assert = require('assert'),
    UserController = require('../../api/controllers/UserController');

describe("controllers:UserController", function () {
    it("Should properly create user", function () {
        assert(false);
    });

    it("Should properly read user", function () {
        assert(false);
    });

    it("Should properly update user", function () {
        assert(false);
    });

    it("Should properly delete user", function () {
        assert(false);
    });
});
