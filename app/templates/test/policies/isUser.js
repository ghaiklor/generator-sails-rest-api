var assert = require('assert'),
    isUser = require('../../api/policies/isUser');

describe("policies:isUser", function () {
    it("TODO: write this test", function () {
        assert(false);
    });
});
