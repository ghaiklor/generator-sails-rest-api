"use strict";

const assert = require('chai').assert;
const Hook = require('../../../api/hooks/<%= name %>Hook');

describe('hooks:<%= name %>', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
