"use strict";

const assert = require('chai').assert;
const sinon = require('sinon');
const Adapter = require('../../../api/adapters/<%= name %>Adapter');

describe('adapters:<%= name %>Adapter', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
