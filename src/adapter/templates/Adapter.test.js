import { assert } from 'chai';
import sinon from 'sinon';
import Adapter from '../../../api/adapters/<%= name %>Adapter';

describe('adapters:<%= name %>Adapter', () => {
  it('Should be tested', () => {
    assert(false);
  })
});
